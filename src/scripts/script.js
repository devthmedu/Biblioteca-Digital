console.log("Este é mais um projeto realizado 2024");

setTimeout(() => {
    for (let index = 0; index < 4005; index++) {
        const elemento = index;
        console.log("Este é o número do índice " + index);
    }
}, 100);

console.log('concluído impressão');
